using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class layser_hit : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject layserPoint;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
